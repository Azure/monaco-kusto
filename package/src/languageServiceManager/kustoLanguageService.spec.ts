import { test, describe, expect } from '@jest/globals';

describe('getReferencedGlobalParams', () => {
    test('maximumDepthExceeded', () => {
        const languageService = KustoLanguageService;
    });
});
